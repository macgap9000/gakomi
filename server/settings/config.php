<?php

    // Parametry połączeniowe do serwera bazy danych:

        // Adres i port do serwera bazy danych:
        $db_hostaddress = "localhost";
        $db_port = "3306";

        // Login i hasło do bazy danych:
        $db_user = "root";
        $db_password = "";

        // Nazwa bazy danych:
        $db_name = "gakomi";

        // Nazwa źródła danych (DSN = Data Source Name):
        $dsn = "mysql:host=$db_hostaddress;port=$db_port;dbname=$db_name;charset=utf8;";

?>